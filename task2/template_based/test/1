#include "Container.hpp"
int main () {
    
    Container<int> container(10);//  = new Container(10);
    container.AddElements(22);
    int* l = new int[2]{2,3}; 
    container.AddElements(2,l);
    delete[] l;
    container.GetNumberStored();
    //delete container; 

    Container<float> contflo(220);
    return 0;
};
